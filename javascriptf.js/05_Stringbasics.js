const myname = "Sonu"
const repco = 77
// console.log(`my name is ${myname} and my repo count is ${repco}`);

const NewString = new String("hardik")
console.log(NewString.length);



// ***************** NOTES on String Basics *****************
/* There are many methods in string prototype which we can use on strings
1. length => gives length of string
2. toUpperCase() => converts string to uppercase
3. toLowerCase() => converts string to lowercase
4. slice(startIndex, endIndex) => extracts part of string from startIndex to endIndex-1
5. replace(oldSubstr, newSubstr) => replaces oldSubstr with newSubstr in the string
6. concat(str1, str2, ...) => concatenates multiple strings
7. includes(substr) => checks if substr is present in the string, returns true/false

   etc.......*/